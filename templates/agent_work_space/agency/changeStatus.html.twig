{{ form_start(form, {'attr': {'id':'entry-form-'~agencyAgent.id, 'onsubmit':'return saveEditAgentForAgency('~ agencyAgent.id ~');'} }) }}
<div class="modal-body ps-5 pe-5">
    <div id="flash-message-overlay">
    </div>
    <div class="row" id="edit-button-area">
        <div class="col">
            <h4 class="pb-2">
                <small>
                    <a href="#" onclick="return enableEditForm({{ agencyAgent.id }})"
                       title="Éditer" class="float-end text-warning">
                        <i class="mdi mdi-lock"></i></a>
                </small>
            </h4>
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{ include('agent_work_space/agency/changeStatus_form.html.twig') }}
        </div>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-danger btn-sm" data-bs-dismiss="modal">Annuler</button>
    <input type="submit" value="Enregistrer" id="entry-submit-{{ agencyAgent.id }}"
           class="btn btn-success btn-sm d-none"/>
</div>
{{ form_end(form) }}