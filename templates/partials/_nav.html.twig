

<!-- Top Header -->
<nav class="navbar navbar-expand-lg navbar-light p-0 osahan-nav border-bottom bg-white">
    <div class="container-fluid px-lg-5">
        <a class="navbar-brand" href="{{ path('app_agent_work_space') }}">
            <img src="{{ asset('assets/img/smart-immo-l.png') }}" alt="#" class="img-fluid">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup16" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup16">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 mx-4">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathinfo == "/" %} active{% endif %}" aria-current="page" href="#"></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathinfo == "/listing" %} active{% endif %}" aria-current="page" href="#"></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathinfo == "/listing/airbnb" %} active{% endif %}" aria-current="page" href="#"></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{% if app.request.pathinfo == "/customer/recommandation" %} active{% endif %}" aria-current="page" href="#">

                        <span class="badge bg-danger ms-2 animate__animated animate__tada animate__infinite" style="font-size: 0.8em; vertical-align: middle;"></span>
                    </a>
                </li>
                <li class="nav-item dropdown">

                    <ul class="dropdown-menu border-0 shadow-sm">
                        <li><a class="dropdown-item" href="#"> </a></li>
                        <li><a class="dropdown-item" href="#"> </a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathinfo == "/contact/us" %} active{% endif %}" aria-current="page" href="#"></a>
                </li>
            </ul>
            {% if is_granted('IS_AUTHENTICATED') %}
                <div class="navbar-nav gap-2">
                 <span class="top-user-dropdown dropdown me-2">
                    <button type="button" class="btn dropdown-toggle" id="dropdownMenuOffset" data-bs-toggle="dropdown" aria-expanded="false" data-bs-offset="10,20">
                        {% if app.user.picture == null %}
                            <img src="{{ asset('assets/img/imageIcon.png') }}" class="rounded img-fluid user-img" alt="">
                        {% else %}
                            <img src="{{ app.user.picture }}" class="rounded img-fluid user-img" alt="">
                        {% endif %}
                        {{ app.user.name }}
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuOffset">

                        <li><a class="dropdown-item" href="{{ path('app_account', {show: 'profil'}) }}">Profile</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    </ul>
                </span>
                </div>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn bg-white btn-sm ps-2 pe-3 py-2 rounded-9 text-muted">
                    <span class="mdi mdi-account bg-light text-muted small me-2 btn-user"></span>Se connecter
                </a>

            {% endif %}
{#            <a href="https://app.smartimmo-rdc.com/" target="_blank" class="btn btn-sm px-3 py-2 my-auto rounded-9 text-white" style="background-color: #28447c">Espace gestion</a>#}
        </div>
    </div>
</nav>
<!-- Top Header -->
